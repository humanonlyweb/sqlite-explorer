{
  "name": "sqlite-explorer",
  "displayName": "SQLite Explorer",
  "version": "0.2.4",
  "description": "Browse, query, and edit SQLite databases directly in VS Code.",
  "categories": [
    "Data Science",
    "Other",
    "Visualization"
  ],
  "keywords": [
    "database",
    "editor",
    "sql",
    "sqlite",
    "viewer"
  ],
  "homepage": "https://github.com/humanonlyweb/sqlite-explorer#readme",
  "bugs": {
    "url": "https://github.com/humanonlyweb/sqlite-explorer/issues"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/humanonlyweb/sqlite-explorer.git"
  },
  "qna": "https://github.com/humanonlyweb/sqlite-explorer/discussions",
  "publisher": "HumanOnlyWeb",
  "workspaces": [
    "webview"
  ],
  "type": "module",
  "main": "./dist/extension.js",
  "scripts": {
    "vscode:prepublish": "bun run build",
    "build": "bun run build:ext && bun run build:web",
    "build:ext": "node rolldown.build.ts --production",
    "build:web": "bun run --filter @sqlite-explorer/webview build",
    "watch": "node rolldown.build.ts --watch",
    "watch:web": "bun run --filter @sqlite-explorer/webview watch",
    "package": "node scripts/package.mjs",
    "test": "node --test src/*.test.ts",
    "typecheck": "bun run typecheck:ext && bun run typecheck:web",
    "typecheck:ext": "tsc --noEmit",
    "typecheck:web": "bun run --filter @sqlite-explorer/webview typecheck",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "format": "oxfmt",
    "format:check": "oxfmt --check",
    "prepare": "simple-git-hooks"
  },
  "dependencies": {
    "better-sqlite3": "^13.0.1"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^26.1.1",
    "@types/vscode": "^1.125.0",
    "@vscode/vsce": "^3.9.2",
    "nano-staged": "^1.0.2",
    "ovsx": "^1.0.2",
    "oxfmt": "^0.60.0",
    "oxlint": "^1.75.0",
    "oxlint-tsgolint": "^7.0.2001",
    "rolldown": "^1.2.0",
    "simple-git-hooks": "^2.13.1",
    "typescript": "npm:typescript-native-bridge@6.0.3-bridge.6.tsgo.7.0.2"
  },
  "overrides": {
    "typescript": "npm:typescript-native-bridge@6.0.3-bridge.6.tsgo.7.0.2"
  },
  "simple-git-hooks": {
    "pre-commit": "bunx nano-staged"
  },
  "nano-staged": {
    "*.{js,ts,vue}": [
      "bunx oxlint --fix",
      "bunx oxfmt --write"
    ],
    "*.{scss,css,json}": "bunx oxfmt --write"
  },
  "contributes": {
    "customEditors": [
      {
        "viewType": "sqliteExplorer.editor",
        "displayName": "SQLite Explorer",
        "selector": [
          {
            "filenamePattern": "*.db"
          },
          {
            "filenamePattern": "*.sqlite"
          },
          {
            "filenamePattern": "*.sqlite3"
          },
          {
            "filenamePattern": "*.db3"
          }
        ],
        "priority": "default"
      }
    ],
    "commands": [
      {
        "command": "sqliteExplorer.open",
        "title": "SQLite Explorer: Open Database…"
      }
    ],
    "configuration": {
      "title": "SQLite Explorer",
      "properties": {
        "sqliteExplorer.pageSize": {
          "type": "number",
          "default": 1000,
          "minimum": 50,
          "maximum": 100000.0,
          "description": "Number of rows fetched per page from the database."
        },
        "sqliteExplorer.readOnly": {
          "type": "boolean",
          "default": false,
          "description": "Open databases in read-only mode (disables editing, inserts, and deletes)."
        },
        "sqliteExplorer.checkForUpdates": {
          "type": "boolean",
          "default": true,
          "description": "Check GitHub Releases for a newer version at most once per day when SQLite Explorer activates."
        }
      }
    }
  },
  "activationEvents": [],
  "icon": "images/icon.png",
  "engines": {
    "vscode": "^1.125.0"
  },
  "packageManager": "bun@1.3.14"
}
